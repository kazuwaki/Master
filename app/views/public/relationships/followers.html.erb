<div class="container">
  <div class="row">
    <div class="col-10 offset-1 mt-5">
    <h2>フォロワー</h2>
      <% if @customers.exists? %>
        <%= render 'public/relationships/follow_customer', customers: @customers %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
   </div>
  </div>
</div>